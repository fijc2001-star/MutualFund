"""Module M6 — Risk & Sizing (ARCHITECTURE §3.4, REQUIREMENTS §5.6).

Sizes signals into orders (`sizing`), checks orders against portfolio limits (`model`),
and enforces hard, bot-unoverridable account guardrails (`guardrails`).
"""
